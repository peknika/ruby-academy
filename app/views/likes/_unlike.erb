<%= form_with(model: @like, url: post_likes_path(post), method: :delete) do |f| %>
  <div><%= hidden_field_tag :post_id, post.id %></div>
  <%= f.button :submit, class: "btn btn-link liked" do %>
    <%= fa_icon("heart") %>
  <% end %>
<% end %>
