<%= form_with(model: current_user.active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :id, @user.id %></div>
  <%= f.button :submit do %>
    <%= fa_icon "heart" %>
  <% end %>
<% end %>