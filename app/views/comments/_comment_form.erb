<%= form_with(scope: :comment, url: post_comments_path(post), method: :post) do |f| %>
  <%= f.text_field :content, class: 'form-control border-0 comment-input', placeholder: 'Add a comment...' %>
  <%= f.submit 'Post', class: 'btn btn-outline-secondary btn-block' %>
<% end %>